export const USER_API = '/api/user';

export const QUESTIONS_API = '/api/questions';

export const DOMAINS = [
    'bg',
    'com'
];

export const USERNAME_LENGTH = { maxCount: 12, minCount: 5 };

export const EMAIL_LENGTH = { maxCount: 20, minCount: 10 };

export const PASSWORD_LENGTH = { maxCount: 20, minCount: 9 };